# Spring Boot suite — full rerun, 2026-07-31

First **full** suite rerun (all 1975 classes, not a residual subset) since
the original 2026-07-11 baseline, after merging `dev` forward and
rebuilding.

- **Worktree:** `C:\craton\CratonVM-spring-boot-residual-20260728`, branch
  `feat/spring-boot-residual-rerun-20260728`, merged forward to `dev` @
  `30278c550`.
- **Binary:** `cratonvm-spring-boot-residual0728.exe` (rebuilt).
- **Sharding:** 4 shards (452-568 classes each, evenly split from the
  1975-class `all-tests.tsv`), `-Parallel 4` per shard (16 concurrent
  CratonVM processes total — same aggregate concurrency as the original
  8-shard/`-Parallel 2` baseline runs), `-TimeoutSec 300`,
  `RunName=craton-fullsuite-20260731`.

## Totals — vs. the original 2026-07-11 baseline

| Status | 2026-07-11 | 2026-07-31 | Δ |
|---|---:|---:|---:|
| PASS | 1225 (62.0%) | **1883 (95.3%)** | **+658** |
| FAIL | 508 (25.7%) | 20 (1.0%) | **-488** |
| HANG | 150 (7.6%) | 26 (1.3%) | **-124** |
| CRASH | 49 (2.5%) | 3 (0.15%) | **-46** |
| EMPTY | 43 (2.2%) | 43 (2.2%) | 0 (unchanged — structurally empty test fixtures, not a CratonVM gap) |
| **Total** | **1975** | **1975** | |

**95.3% of the full Spring Boot 4.1.0-SNAPSHOT / Spring Framework 7.0.7
suite now passes on CratonVM**, up from 62.0% three weeks ago. This
reflects the cumulative effect of dozens of fixes landed by this
investigation thread and many concurrent sessions on this shared host
across the 07-11 → 07-31 window (see
`docs/known-issues/springboot/` and `fixed-suite-bugs/springboot/`
for the individual bug docs/fix history).

## Residual (49 non-PASS/non-EMPTY: 20 FAIL / 26 HANG / 3 CRASH)

### 3 CRASH — worth immediate attention

| Module | Class |
|---|---|
| `module/spring-boot-devtools` | `RestartClassLoaderTests` |
| `module/spring-boot-security` | `JerseyEndpointRequestIntegrationTests` |
| `module/spring-boot-webmvc` | `BasicErrorControllerIntegrationTests` |

`BasicErrorControllerIntegrationTests` is the same class as
`basicerrorcontroller-class-cluster-20260728-FIXED.md` (retired 2026-08-01)
(already fixed once, per the 07-30 merge, then apparently regressed
again, or a new distinct crash — not re-diagnosed this round). The other
2 are new findings this round, not investigated.

### By module (non-PASS/non-EMPTY, top clusters)

| Module | Count |
|---|---:|
| `core/spring-boot` | 8 |
| `module/spring-boot-webmvc` | 3 |
| `module/spring-boot-jooq-test` | 3 |
| `module/spring-boot-webflux` | 2 |
| `module/spring-boot-security` | 2 |
| `module/spring-boot-jooq` | 2 |
| `module/spring-boot-integration` | 2 |
| `module/spring-boot-devtools` | 2 |
| `module/spring-boot-cloudfoundry` | 2 |
| `module/spring-boot-cache` | 2 |
| `module/spring-boot-actuator` | 2 |
| `core/spring-boot-autoconfigure` | 2 |
| (18 other modules) | 1 each |

Not triaged against `docs/known-issues/springboot/`'s existing doc set
this round — a full-suite rerun-only pass. Given how much of the doc
corpus was retired/fixed since 07-28 (confirmed during that round's
merge), most of these 49 residuals are very likely a mix of genuinely new
long-tail issues and a few regressions — worth a dedicated triage pass
before assuming any particular one is new or already covered.

## Reproduce / rerun

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-25.0.3.9-hotspot"
$exe = "<worktree>\target\release\cratonvm-spring-boot-residual0728.exe"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File apps\spring-boot-suite-runner\run-spring-boot-suite.ps1 `
  -Vm craton -Exe $exe -JdkHome $env:JAVA_HOME -Category all `
  -RunName <name> -Parallel 4 -TimeoutSec 300
```

Full results: `apps/spring-boot-suite-runner/.suite/results/craton-fullsuite-20260731/shard{1..4}/results.tsv`.
