# Spring Boot suite rerun — 2026-07-23

Reran the 429 classes confirmed CratonVM-specific in the 2026-07-17 rerun
(304 FAIL / 120 HANG / 5 CRASH — see `RESULTS-20260717.md` and
`docs/known-issues/springboot/README.md`'s 2026-07-17 section) against
current `dev`, after merging and rebuilding. `dev` moved substantially in
the 6 days between rounds.

- **Worktree:** `C:\craton\CratonVM-spring-boot-rerun-20260717`, branch
  `feat/spring-boot-suite-rerun-20260717`, merged forward to `dev` @
  `7b7ad226d` before this run.
- **Binary:** `cratonvm-spring-boot-rerun0717.exe` (rebuilt).
- **Sharding:** 8 shards (~52-57 classes each, class list shuffled first
  so HANG classes are spread evenly rather than clustered in one shard),
  `-Parallel 2` per shard (16 concurrent CratonVM processes),
  `-TimeoutSec 300`, `RunName=craton-rerun-20260723`.

## Totals — before vs. after

| Status | 2026-07-17 | 2026-07-23 | Δ |
|---|---:|---:|---:|
| PASS | 0 (by definition — this was the non-passing set) | 290 (67.6%) | **+290** |
| FAIL | 304 | 99 | **-205** |
| HANG | 120 | 40 | **-80** |
| CRASH | 5 | 0 | **-5 (all resolved)** |
| **Total** | **429** | **429** | |

**290 of the 429 classes confirmed CratonVM-specific 6 days ago now
PASS** — a large fraction of the fixes landing during this window's very
active concurrent development on this shared host (the `%n` line-separator
fix from this same investigation thread, the `dispose()`/`shutdown()`
`Class.getMethods()` hierarchy-dedup fix, and many others outside this
thread's own visibility — `dev` had dozens of intervening commits).

**All 5 previously-CRASH classes are confirmed resolved** — 4 now `PASS`
(`SslServerCustomizerTests`, `BraveBaggagePropagationIntegrationTests`,
`ModifiedClassPathExtensionForkParameterizedTests`,
`ModifiedClassPathExtensionOverridesParameterizedTests`), 1 now `FAIL`
(`OpenTelemetryBaggagePropagationIntegrationTests` — no longer a fatal
crash, but still not passing; likely a residual of the same
`NoClassDefFoundError: ExceptionUtils` cluster documented in
`docs/known-issues/springboot/crashfail-20260717-crash-cluster.md`, worth
a follow-up look rather than assuming fully resolved).

## Residual (139 classes: 99 FAIL / 40 HANG) — top modules

| Module | Residual count |
|---|---:|
| `core/spring-boot` | 39 |
| `loader/spring-boot-loader` | 15 |
| `module/spring-boot-jdbc` | 6 |
| `module/spring-boot-tomcat` | 5 |
| `module/spring-boot-security` | 4 |
| `module/spring-boot-graphql` | 4 |
| `module/spring-boot-devtools` | 4 |
| `module/spring-boot-data-redis` | 4 |
| `module/spring-boot-mongodb` | 3 |
| `module/spring-boot-micrometer-tracing-opentelemetry` | 3 |
| `module/spring-boot-micrometer-metrics` | 3 |
| `module/spring-boot-jetty` | 3 |

Not re-triaged against `docs/known-issues/springboot/`'s 99 docs from the
07-17 round this session — many of the residual FAILs/HANGs are very
likely the same already-documented clusters (the `%n` fix and
`getMethods()` fix both landed, but the JUnit5 `InterceptingExecutableInvoker`
livelock cluster, the `ModifiedClassPathExtension` network-hang cluster,
and the PKCS12/PEM keystore cluster were all still OPEN/unconfirmed as of
07-17, and this round's HANG count (40) and module distribution look
broadly consistent with those clusters persisting rather than new ones
appearing). Worth a dedicated re-triage pass to confirm which docs can be
closed vs. which are still live, rather than assuming.

## Reproduce / rerun

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-25.0.3.9-hotspot"
$exe = "<worktree>\target\release\cratonvm-spring-boot-rerun0717.exe"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File apps\spring-boot-suite-runner\run-spring-boot-suite.ps1 `
  -Vm craton -Exe $exe -JdkHome $env:JAVA_HOME -ClassList <shard-class-list.tsv> `
  -RunName <name> -Parallel 2 -TimeoutSec 300
```

Full 429-class reference list (module+class, no status):
`apps/spring-boot-suite-runner/.suite/actionable-20260717.tsv`.
