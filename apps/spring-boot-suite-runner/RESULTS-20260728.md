# Spring Boot suite rerun — 2026-07-28/29

Reran the 139-class residual from the 2026-07-23 rerun (99 FAIL + 40 HANG)
against current `dev`, after merging and rebuilding, at **5x the normal
timeout (1500s vs. 300s)** to separate genuinely-stuck classes from
just-slow ones — same methodology as the original 2026-07-11 HANG-rerun.

- **Worktree:** `C:\craton\CratonVM-spring-boot-residual-20260728`, branch
  `feat/spring-boot-residual-rerun-20260728`, off `dev` @ `8a060636e`. This
  is a **new worktree**, not a continuation of
  `feat/spring-boot-suite-rerun-20260717` — that worktree's git
  registration was pruned by a concurrent disk-cleanup sweep between
  sessions (files survived, `.git` link didn't; see
  [[feedback_shared_host_disk_cleanup]] for the recovery). All prior work
  from that branch was already merged into `dev` and pushed, so nothing
  was lost — this worktree reused the old one's `apps/spring-boot` checkout
  and `.suite` data via `robocopy` rather than a full re-seed.
- **Binary:** `cratonvm-spring-boot-residual0728.exe` (rebuilt).
- **Sharding:** 2 shards (~70 classes each), `-Parallel 2` per shard,
  `-TimeoutSec 1500`, `RunName=craton-rerun-20260728`.

## Totals — before vs. after

| Status | 2026-07-23 (input, @300s) | 2026-07-28 (@1500s) | Δ |
|---|---:|---:|---:|
| PASS | 0 (by definition) | 116 (83.5%) | **+116** |
| FAIL | 99 | 20 | **-79** |
| HANG | 40 | 1 | **-39** |
| CRASH | 0 | 2 | **+2 (new)** |
| **Total** | **139** | **139** | |

**116 of the 139 classes still failing 6 days ago now PASS** — dev moved
substantially again in that window (confirms the pattern from the
07-17→07-23 round: this shared host has very active concurrent fix
throughput). The HANG count dropping from 40 to 1 at 5x timeout mirrors
the original 2026-07-11 HANG-rerun finding (most HANGs are slow-but-
eventually-completing, not truly stuck) combined with real fixes landing.

## 2 new CRASH — worth immediate attention

Both are process-fatal, neither was a CRASH on 07-23 (both were FAIL
before):

- **`module/spring-boot-webmvc` `BasicErrorControllerIntegrationTests`**
  — this is the exact class from
  `docs/known-issues/springboot/http-parse-url-query-only-authority-split-regression-20260723.md`
  (a fix that was silently reverted by an intervening merge). Now crashes
  fatally instead of failing cleanly — likely a further regression in the
  same area, not investigated this session. Log:
  `apps/spring-boot-suite-runner/.suite/results/craton-rerun-20260728/shard2/logs/module_spring-boot-webmvc.org.springframework.boot.webmvc.autoconfigure.error.BasicErrorCon-957a1d0f4289.err.log`.
- **`core/spring-boot` `OriginTrackedYamlLoaderTests`** —
  `OutOfMemoryError: Java heap space (alloc_array length 1026)`. This
  exact class was flagged back in the very first 2026-07-11 run as "a
  runner classpath artifact (pathing-jar manifest truncation suspected),
  not a CratonVM bug — not filed." A genuine OOM with a specific,
  suspiciously-small allocation size (1026 elements) is a different, more
  concrete symptom than the original vague suspicion — worth a fresh look
  rather than assuming the old dismissal still applies. Log:
  `apps/spring-boot-suite-runner/.suite/results/craton-rerun-20260728/shard2/logs/core_spring-boot.org.springframework.boot.env.OriginTrackedYamlLoaderTests.err.log`.

## 1 remaining HANG — confirmed genuinely stuck

`module/spring-boot-jooq` `JooqAutoConfigurationTests` — timed out at the
full 1500s. This is `jooq-destroy-method-ambiguity-and-hang.md`'s
"Cluster B" (already OPEN/unconfirmed as of 07-17) — confirms it's a real
stuck condition, not a too-short-timeout artifact.

## Residual (23 classes: 20 FAIL / 2 CRASH / 1 HANG) — by module

| Module | Count |
|---|---:|
| `core/spring-boot` | 3 |
| `module/spring-boot-mongodb` | 2 |
| `module/spring-boot-jetty` | 2 |
| `module/spring-boot-data-redis` | 2 |
| (16 other modules) | 1 each |

Not re-triaged against the existing `docs/known-issues/springboot/` doc
set this session — this was a rerun-only pass per the request. Most of
the 20 residual FAILs are very likely already-documented clusters from the
07-17/07-23 rounds; worth a confirmation pass before assuming any are new.

## Reproduce / rerun

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-25.0.3.9-hotspot"
$exe = "<worktree>\target\release\cratonvm-spring-boot-residual0728.exe"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File apps\spring-boot-suite-runner\run-spring-boot-suite.ps1 `
  -Vm craton -Exe $exe -JdkHome $env:JAVA_HOME -ClassList <shard-class-list.tsv> `
  -RunName <name> -Parallel 2 -TimeoutSec 1500
```

Full 139-class input list:
`apps/spring-boot-suite-runner/.suite/residual-20260723.tsv`.
