# Strict-corpus divergence baseline — jdk 25, linux
#
# GENERATED by scripts/jdk-only-strict-probes.sh --update-baseline.
# Never hand-edited: every line is a (probe, arm, section) that a real
# three-arm run measured as diverging from the HotSpot control.
#
# note: the refused snapshot iterator and stream collector are fixed: HashMap$KeyItr/TreeSet$Itr fall back to a real Arrays$ArrayItr and the StreamCollector drain goes through Spliterators.iterator, retiring 17 sections. JdkOnlyPlatformProbe/*/vthreads stays baselined — it is the documented intermittent section and a first re-freeze landed on the rare run where the real arm matched, writing a baseline too tight to hold
#
# The gate fails when this set GROWS. A line that stops diverging is
# reported and passes, so a fix is never blocked by the gate that
# measured it — re-freeze afterwards to keep the ratchet tight.
JdkOnlyBreadthProbe/strict/PROBE2
JdkOnlyBreadthProbe/strict/SECTION-FAILED:serialization
JdkOnlyBreadthProbe/strict/serialization
JdkOnlyPlatformProbe/real/agent
JdkOnlyPlatformProbe/real/jni
JdkOnlyPlatformProbe/real/security
JdkOnlyPlatformProbe/real/vthreads
JdkOnlyPlatformProbe/strict/agent
JdkOnlyPlatformProbe/strict/jni
JdkOnlyPlatformProbe/strict/security
JdkOnlyPlatformProbe/strict/vthreads
