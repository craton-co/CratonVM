{
  "$schema_version": 1,
  "generated_at": "2026-04-24T00:00:00Z",
  "rustjvm_version": "unknown",
  "fixture": {
    "mode": "placeholder",
    "placeholder_source": "apps/ejbca_min_fixture/src/Main.java",
    "real_source_expected_at": "C:/craton/ejbca-test-run/src",
    "notes": "Placeholder is used until the full cesecore AccessMatchType + DirectRunner subset is staged. stage-ejbca-min.sh auto-selects real over placeholder when C:/craton/ejbca-test-run/src/org/cesecore/authorization/user/AccessMatchType.java is present."
  },
  "steps": [
    {
      "name": "stage",
      "rc": 0,
      "notes": "javac --release 21 of Main.java (placeholder) or AccessMatchType.java + DirectRunner.java (real). Depends only on a JDK 21+ javac on PATH or JAVA_HOME."
    },
    {
      "name": "run",
      "rc": 1,
      "notes": "rust-jvm currently throws NullPointerException on the 2nd System.out.println (WP0.1 blocker, session 93). Placeholder Main.java triggers this intentionally. Once WP0.1 lands this rc flips to 0 and the expected_stderr_contains list shrinks; expected_stdout_contains gains 'ejbca_min_fixture: ok'."
    }
  ],
  "expected_stderr_contains": [
    "NullPointerException",
    "Cannot invoke write on null"
  ],
  "expected_stderr_forbidden": [
    "panicked at",
    "RUST_BACKTRACE",
    "abort",
    "STATUS_ACCESS_VIOLATION"
  ],
  "expected_stdout_contains": [
    "ejbca_min_fixture: starting baseline smoke"
  ],
  "expected_final_rc": 1,
  "known_blockers": [
    {
      "id": "WP0.1",
      "summary": "Two consecutive System.out.println NPE on 2nd write ('Cannot invoke write on null')",
      "see": "memory/finding_println_regression.md; docs/wildfly-ejbca-roadmap.md WP0.1"
    },
    {
      "id": "WP0.2",
      "summary": "ObjectStreamClass.serializableConstructor null — blocks JUnit4 harness",
      "see": "docs/wildfly-ejbca-roadmap.md WP0.2"
    }
  ],
  "update_policy": "Update this file any time a WP lands that changes expected_final_rc or the expected_stderr_* sets. diff-baseline.sh compares a fresh run against these fields. A 'baseline match' result is mandatory for CI green; a mismatch either indicates a regression (bad) or an unshipped fix (update this file in the same PR)."
}
