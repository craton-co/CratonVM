# HotSpot oracle for probes/CollectionSerProbe.java
#
# Temurin 25.0.3+9 (/data/toolchain/jdk-25), captured 2026-08-20.
#
# 20 of these 52 rows failed on CratonVM before `native_imm_*_write_replace` /
# `native_collser_read_resolve` (native-collections) and their two force-native
# gate entries. Every failing row was a shape whose real JDK class declares a
# `writeReplace()`: the six `ImmutableCollections$*` carriers behind
# `List.of`/`Set.of`/`Map.of`/`copyOf`, plus
# `Collections$UnmodifiableRandomAccessList`. Those bodies read the class's own
# declared fields, which a CratonVM-minted carrier never fills, so the
# `java.util.CollSer` handed to the stream carried this VM's internal slots:
#
#   List.of("a")                 round-tripped to [[a], 1]   (backing, marker)
#   Set.of("a","b","c")          InvalidObjectException: invalid object
#   Map.of("a","1")              NullPointerException: ... "this.table" is null
#   unmodifiableList(ArrayList)  NullPointerException
#
# The `unmodifiableList(LinkedList)` row is the control for the last of those:
# the same wrapper family, no `writeReplace`, and it passed throughout.

== List.of / Set.of / Map.of
OK   List.of() => []
OK   List.of(1) => [a]
OK   List.of(2) => [a, b]
OK   List.of(3) => [a, b, c]
OK   List.of(5) => [a, b, c, d, e]
OK   Set.of() => []
OK   Set.of(1) => [a]
OK   Set.of(2) => [a, b]
OK   Set.of(3) => [a, b, c]
OK   Set.of(5) => [a, b, c, d, e]
OK   Map.of() => {}
OK   Map.of(1) => {a=1}
OK   Map.of(2) => {a=1, b=2}
OK   Map.of(3) => {a=1, b=2, c=3}
OK   Map.ofEntries(2) => {a=1, b=2}
== copyOf
OK   List.copyOf => [a, b, c]
OK   Set.copyOf => [a, b, c]
OK   Map.copyOf => {a=1, b=2, c=3}
== Collections.unmodifiable*
OK   unmodifiableList => [a, b, c]
OK   unmodifiableList(LinkedList) => [a, b, c]
OK   unmodifiableCollection => [a, b, c]
OK   unmodifiableSet => [a, b, c]
OK   unmodifiableSortedSet => [a, b, c]
OK   unmodifiableNavigableSet => [a, b, c]
OK   unmodifiableMap => {a=1, b=2, c=3}
OK   unmodifiableSortedMap => {a=1, b=2, c=3}
== Collections.synchronized*
OK   synchronizedList => [a, b, c]
OK   synchronizedList(LinkedList) => [a, b, c]
OK   synchronizedSet => [a, b, c]
OK   synchronizedMap => {a=1, b=2, c=3}
== Collections singletons / empties
OK   emptyList => []
OK   emptySet => []
OK   emptyMap => {}
OK   singletonList => [a]
OK   singleton => [a]
OK   singletonMap => {a=1}
OK   nCopies => [a, a, a]
== plain collections (control)
OK   ArrayList => [a, b, c]
OK   LinkedList => [a, b, c]
OK   HashSet => [a, b, c]
OK   LinkedHashSet => [a, b, c]
OK   TreeSet => [a, b, c]
OK   HashMap => {a=1, b=2, c=3}
OK   LinkedHashMap => {a=1, b=2, c=3}
OK   TreeMap => {a=1, b=2, c=3}
OK   Hashtable => {a=1, b=2, c=3}
OK   ArrayDeque->list => [a, b, c]
OK   Arrays.asList copy => [a, b]
== nested (the Spring shape)
OK   List.of inside ArrayList => [[a], {k=v}, [x]]
OK   unmodifiableList of List.of => [a, b]
TOTALFAILS=0
