# HotSpot oracle for probes/SelfRecCatchProbe.java
#
# Temurin 25.0.3+9 (/data/toolchain/jdk-25), captured 2026-08-20.
#
# Before the fix, CratonVM did not produce a WRONG row here — it exited 1 with
#
#   Exception in thread "main" java.lang.NoSuchMethodException:
#     java.lang.Object.run(java.lang.String)
#       at SelfRecCatchProbe.hasUsableImplementation(SelfRecCatchProbe.java)
#       at SelfRecCatchProbe.hasUsableImplementation(SelfRecCatchProbe.java)
#
# i.e. an exception escaping a `catch (NoSuchMethodException)` that covers the
# call that threw it. Two compiled activations of one self-recursive method
# share `jit_set_throw_bci`'s single per-thread slot, the outer one's stamp (its
# own recursive call site, outside the try) overwrites the inner one's (the real
# throw site, inside it), and the drain concludes the method cannot catch it.
#
# `--nojit` and `CRATONVM_JIT_SELF_TAILCALL=1` both hide it: neither produces two
# compiled activations. The rows below are identical under all three.
#
# The per-class fields are SORTED — `getDeclaredMethods()` order is unspecified
# and the two VMs legitimately differ on it.

A -> other=false visit=false
B -> other=false visit=true
C -> other=false visit=true
D -> other=true visit=true
E -> run=false
F -> other=false visit=false
TRUECOUNT=4
