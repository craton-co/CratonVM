# HotSpot oracle for probes/VarargsCollectorProbe.java
#
# Temurin 25.0.3+9 (/data/toolchain/jdk-25), captured 2026-08-20.
#
# 10 of these 15 rows failed on CratonVM before the varargs-collector marking in
# `alloc_method_handle` (`method_is_variable_arity`) and the `asType` acceptance
# in `mh_astype_refusal` (`varargs_astype_is_legal`), both in
# native-builtins/src/lang_invoke.rs, plus the array spelling fix in
# `class_display_simple_name` that this probe's `findStatic.type` row measures.
#
# If a row here regresses on a later build, suspect the OTHER open defect first:
# `asType` on this VM still adapts THE RECEIVER IN PLACE instead of returning a
# new handle (the G31-1 nomination recorded on the `asType` registration), so an
# earlier row can leave behind a handle a later row then reads.
# `probes/MhCombinatorProbe.java`'s purity section measures that directly.

OK   findStatic.isVarargsCollector => true
OK   findStatic.type => (String,String[])String
OK   static asType(+2) => a|b|c
OK   static asType(+0) => a
OK   static invoke (inexact, collects) => a|b|c
OK   findVirtual.isVarargsCollector => true
OK   virtual asType(+2) => i:a|b|c
OK   virtual invoke (inexact, collects) => i:a|b
OK   unreflect.isVarargsCollector => true
OK   unreflect asType(+2) => a|b|c
OK   asFixedArity.isVarargsCollector => false
OK   hand-made collector.isVarargsCollector => true
OK   hand-made collector asType(+2) => a|b|c
OK   fixed.isVarargsCollector => false
OK   varargs ctor collector => true
TOTALFAILS=0
