// Version 1: `C.x` is an Object. Compile into `c1c`; this is what StrictMain
// is compiled against, so the fieldref records `C.x:Ljava/lang/Object;`.
public class C { public Object x = "C.x-object"; }
