# Targets in vm/tests that RUN when their prerequisites are present.
#
# `vm/tests/common/mod.rs` turns a missing `cratonvm` binary, JDK or Java
# fixture into an `eprintln!` + `return`, at which point cargo prints
# `test ... ok`. A whole suite can report green while executing none of its
# assertions -- and 95 of the 174 files in vm/tests use that helper.
#
# CRATONVM_REQUIRE_E2E=1 turns every such skip into a panic. The CI step that
# sets it iterates THIS file, and ci.yml's rule for joining is that a target
# is listed only after someone has run it against a real CratonVM binary.
#
# Regenerate with:
#   CRATONVM_BIN=<binary> JAVA_HOME=<jdk> bash tools/e2e-ratchet-survey.sh
# and move any target the survey reports as RUNS into the list below.
#
# Surveyed 2026-08-30 against a release `cratonvm` and Temurin/TornadoVM
# JDK 25.0.3: 73 RUNS, 19 VACUOUS, 3 RED out of 95.
abstract_collection_to_array_typed
al_itr_lastret_slot_regression
annotation_class_element_null
annotation_loader_isolation
array_receiver_dispatch
arraylist_reverse_list_iterator
block_2b_logmanager_factory
class_loader_unload_regression
clinit_first_call_compile_order
collection_view_gated_surface
conscrypt_logger_dispatch
es_segalloc_arena_dispatch
fjp_recursive
getclass_concrete_class
hib_dev_05_private_abstract_dispatch
inet_address_hostname_contract
inet_socket_address_port_only
inputstream_super_read_probe
instant_real_jdk_factory_tostring
intrinsic_diff
jit_cold_new_cp
jit_collection_ctor_identity
jit_cov06_array_allocation
jit_final_devirt_native_shadow
jit_interp_differential
jit_ir_athrow_dispatch
jit_ir_exception_stub_throw_bci
jit_null_receiver_npe
jit_osr_athrow_lift
jit_osr_throw_outside_try
map_view_remove_if
native_alloc_oom_catchable
native_io_dis_read_fully_pin
native_io_dis_shared_stream
nio_selector_build_set_gc
null_receiver_cached_invoke
osr_unbox_intrinsic_handler_frame
plain_server_socket_contract
private_invokevirtual_shadow
probe_compile_guard
probe_fixture_census
rbigdec1_arithmetic
rbigdec1_full_arithmetic
real_net_ssl_factory_registry
reflective_annotation_array_component
require_e2e_gate
rfjp1_recursive
rslf4j1_get_resources
runtime_halt
sb_count_slot_guard_regression
scheduled_executor_cancel_shutdown_regression
sealed_bootstrap_permitted_subclasses
server_socket_adaptor_accept
socket_input_stream_timeout
spring_io_enumset_regression
sslserversocket_accept_echo
stackwalker_reflect_gc
sublist_view_regression
synchronized_wrapper_mutex
synthetic_diff
threadpoolexecutor_prestart_regression
unmodifiable_list_get_nonarraylist_backing
url_classloader_resource_delegation
wave1_a_jmx_mxbeans
wave1_b2_bootloader_resources
wave1_c_executor
wave1b_get_resource_consistency
wave2_c_methodhandles
wave2_chm
wave3_b2_dispatch
wave3_c_selector
wave3_console_module
wave4_a_atomic
wildcard_bind_under_gc_stress
wp1_9_stackwalker
wp2_5_proxy
writer_flush_real

# ---------------------------------------------------------------------------
# NOT ratcheted: these pass plain and FAIL under CRATONVM_REQUIRE_E2E, so
# their green is the skip rather than the assertions. Listed here because the
# alternative is that they stay invisible -- which is how they got here.
# Each needs its missing prerequisite restored (usually a fixture under the
# gitignored `apps/`, which is why it vanished for everyone but its author).
# Moving one up into the list above is the fix; deleting it from here is not.
#   cluster_a_aqs_chm
#   cluster_b_collection_tostring
#   cluster_c_constructor
#   wave1_d_xml_stax
#   wave2_a_class_getdeclared
#   wave2_bc_probe
#   wave2_bytebuddy
#   wave2_cleaner
#   wave2_h2_connection
#   wave2_string_decode
#   wave2_string_indexof
#   wave3_scanner
#   wave3_spring_boot_fatjar
#   wildfly_jboss_module_service_leak
#   wp2_1_reflect
#   wp2_2_method_invoke_matrix
#   wp2_6_constructor_edges
#   wp2_7_annotation_proxy
#   wp2_9_findspecial

# ---------------------------------------------------------------------------
# NOT ratcheted: FLAKY. Passes and fails with the same binary and the same
# prerequisites, so listing it would make this step red at random -- and a
# step that is red at random is a step nobody reads, which is the failure
# this whole list exists to avoid.
#
#   vthread_probe_regression   3 of 4 runs failed on 2026-08-30. The failing
#                              runs take 60.0s and the passing one 1.7s, so
#                              it is a timeout being hit, not an assertion
#                              disagreeing. Diagnose the 60s wait before
#                              ratcheting it.

# ---------------------------------------------------------------------------
# NOT ratcheted: RED. These RUN against a real binary and FAIL -- a defect,
# not a prerequisite question -- so ratcheting one turns this step red for
# every lane until it is fixed.
#
# NAMED HERE FOR THE REASON THE VACUOUS BLOCK GIVES: "the alternative is that
# they stay invisible -- which is how they got here." Until 2026-09-08 the
# header counted the REDs ("3 RED out of 95") and the file named none of them,
# so a target that RUNS AND FAILS was less visible than one that merely skips.
# Both categories now get a name and a reason.
#
# Re-surveyed 2026-09-08 against a release binary and Temurin 25.0.3+9 on
# Windows: 79 RUNS, 14 VACUOUS, 2 RED out of 95 (was 73/19/3 on 2026-08-30).
# LATER THE SAME DAY one of those two REDs was fixed and ratcheted, leaving
# ONE: `stack_trace_across_tiers`, at the bottom of this block.
#
# PROMOTED 2026-09-08 (second sitting), after `936536100` fixed the RED this
# block used to carry (`null_receiver_cached_invoke`): five of the
# nine candidates this block used to list are now in the ratcheted list above.
# Each was observed RUNning twice -- the morning survey, then again against a
# debug binary built from dev @ ebc5e46c4 -- which is the bar this file asks
# for. `null_receiver_cached_invoke` was run 10 times, not 2, because its row
# is JIT-timing dependent and a single green on such a row is a coin flip:
# 10/10 ok at ~10s each, against FAILED at 4.15s before the fix.
#
#   inet_address_hostname_contract  13s      null_receiver_cached_invoke  10s
#   jit_final_devirt_native_shadow  25s      osr_unbox_intrinsic_...      14s
#   sealed_bootstrap_permitted_subclasses    10s
#
# FOUR of the nine passed the same bar and are still NOT promoted, on cost
# alone. Ratcheting them would add ~5 minutes to a step whose whole list took
# 34m55s here (Windows, debug, idle host) against ci.yml's `timeout-minutes:
# 15`. That gap is not itself an alarm -- the step runs on ubuntu-latest only
# and is green there -- but it is not headroom to spend blind either. Someone
# with the Linux number should take these; the measurement is done:
#
#   jit_guarded_inline_native_shadow          97s
#   jit_ir_phi_copy_register_alias            77s
#   jit_site_trap_never_duplicates_a_side_effect  61s
#   stack_trace_compiled_callee               57s
#
# The ninth, `vthread_probe_regression`, FAILED again on 2026-09-08 at 120s --
# the FLAKY block above is right and its one green that morning was luck.
#
# ---------------------------------------------------------------------------
# TWO TARGETS IN THE RATCHETED LIST ABOVE ARE FLAKY ON WINDOWS. Found by
# running the whole list on 2026-09-08; both then re-run ALONE on an idle host,
# because load manufactures exactly this shape:
#
#   socket_input_stream_timeout   1 of 3 alone. Failing run 86.9s, passing runs
#                                 far under. The fixture is killed by the
#                                 harness clock, not by its assertion.
#   wave1_c_executor              1 of 3 alone, and 1 of 4 again with
#                                 `--test-threads=1`. SERIALISING DOES NOT FIX
#                                 IT, which refutes the obvious reading (five
#                                 #[test] fns each spawning a VM, contending).
#                                 A serialised pass runs all five in ~30s
#                                 total; on a bad run ONE of them alone blows
#                                 the fixture's hardcoded 30s wall. That is a
#                                 5x stall in the VM, not a marginal timeout.
#
# THEY ARE LEFT RATCHETED ON PURPOSE. The FLAKY rule above would remove them,
# but this step runs on `ubuntu-latest` only and every observation here is
# Windows; unratcheting on that evidence would cut real Linux coverage to fix a
# red nobody has seen. What is needed is the same rate measured on Linux. Until
# then they are named here rather than left as a surprise.
#
#   stack_trace_across_tiers   `after_main_osr` loses the `mid`/`outer`
#                              frames: "the callees the JIT inlined into their
#                              caller's artifact contributing no frame at
#                              all". Same family as the null-receiver RED this
#                              block used to carry beside it -- inlining
#                              dropping a JVMS-visible property that the
#                              un-inlined path preserves. That one is now fixed
#                              (`936536100`, an `Op::Guard` ahead of the IR
#                              tier's splice) and ratcheted; this one is not,
#                              and the fix there is not obviously the fix here:
#                              an absent FRAME is not an absent CHECK.
