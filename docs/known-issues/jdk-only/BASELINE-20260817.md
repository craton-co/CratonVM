# BASELINE — 2026-08-17, the first measurement after `dev` was merged

**META record.** Not a defect record. This is the measured state of the suite on
the first binary built after `dev` (432 commits) was merged into
`claude/jdk-only-mode-completion-1351c0`.

## Provenance

| | |
|---|---|
| binary | merge commit `d87dff06a` + `c479a668a` (const-assert) + the `vc_route` arity fix |
| built | 2026-08-17T02:44:03Z, `cargo build --release -p cratonvm-cli`, **14m06s**, `BUILD_EXIT=0` |
| attributable | `find . -name '*.rs' -newermt '<build start>'` returned **empty** |
| oracle | HotSpot 25.0.3+9-LTS, `C:\Program Files\Eclipse Adoptium\jdk-25.0.3.9-hotspot` |
| built in | `C:/craton/cvm-mergecheck`, a detached worktree, so the seven working lanes could keep editing the main tree |

**Every number here is MEASURED.** That is worth stating because most of this
directory is not.

## Results

| arm | invocation | result |
|---|---|---|
| core | `bash run.sh` (the default) | **53 passed, 8 failed of 61** |
| core + RJdk corpus, Compatible mode | `SUITE=all` | **84 passed, 15 failed of 99** |
| `--jdk-only` policy | `CRATONVM_ARGS="--jdk-only"` | see §4 |

### Failing, core arm (8)
`RArrayStoreInterfaces` `RForeignLayoutJdkInterfaces` `RSimpleTimeZoneRaw`
`RImmutableFactoryTypes` `RJdkOptionalShape` `RJdkIntrinsics3` `RJdkBridge1`
`RSslLiveSession`

### Additional failing under `SUITE=all` (7)
`RJdkProxy` `RJdkProxyIface` `RJdkFunctionCombinators` `RJdkForeign`
`RJdkEnumerations` `RJdkMapViews` `RServiceLoaderDoubleSource`

## Three things this measurement corrects

1. **`HANDOFF-20260814` §1 says "two remain red".** That was true of the
   six vectors that session exercised. It is not the suite. The core arm has
   **eight** red, six of which that session never ran.

2. **`SUITE` defaults to `core`.** The 38-vector `RJdk*` corpus — the corpus
   this whole effort is named after — does **not** run unless you pass
   `SUITE=jdk-only` or `SUITE=all`. A green default run says nothing about it.

3. **`SUITE=all` is still not `--jdk-only`.** `run.sh:330` sets `JDK_ONLY=1`
   only when `CRATONVM_ARGS` names the flag; `:585` passes `${CRATONVM_ARGS:-}`
   through to the VM. So `SUITE=all` runs the `RJdk*` corpus in **Compatible**
   mode. The policy arm is a third, separate run. Do not conflate the three.

## First diagnosed failure: `RArrayStoreInterfaces`

MEASURED by diffing both VMs' raw output. **One row of 108** diverges:

```
s12  Runnable[]  <- Proxy(Marker)
  HotSpot   cold=[ArrayStoreException]  hot=[ArrayStoreException]
  CratonVM  cold=[no-throw]             hot=[no-throw]
```
`illegalRefused=11/12` against the oracle's `12/12`; `legalAdmitted=15/15` on both.

It fails **identically in the interpreter (`cold`) and the JIT (`hot`)**, which
rules out both a JIT-lowering bug and an artefact of the `aastore` ABI
unification this merge performed: the *shared* refusal decision is wrong.
Row `s25` (`Marker[] <- Proxy(Marker)`, legal) passes, so proxies are not
ignored wholesale — the predicate admits a proxy into an array of an interface
it does implement, and fails to **refuse** one for an interface it does not.

`RJdkProxy` and `RJdkProxyIface` also fail, so the blast radius is plausibly
three vectors, not one. That is a hypothesis, not a measurement.

## A harness note worth keeping

The suite flagged three vectors that **publish no check count** and are not in
`harness-uncounted.txt` (`RForeignLayoutJdkInterfaces`, `RSimpleTimeZoneRaw`,
`RJdkOptionalShape`, `RJdkIntrinsics3`, `RJdkBridge1`, `RSslLiveSession`).
Without a count, a run that silently asserts FEWER things than the oracle
diffs identically and reads as green. `RSimpleTimeZoneRaw` additionally trips
G2: **nothing survives `extract()`, so its cross-VM diff compares two empty
strings.**

And a trap that cost time here: piping `run.sh` through `tail -70` truncated
the summary and hid an entire arm. Redirect to a file.

## The wrapper exit code lies

`cargo`, `run.sh` and the release build were each reported by the task wrapper
as **exit 0** while their own echoed `BUILD_EXIT` / `CHECK_EXIT` /
`SUITE_EXIT` line said `101`, `101` and `1`. `HANDOFF-20260814` §3 warns about
this; it reproduced three times in one session. Always echo the real code and
read that.

---

## The `--jdk-only` policy arm — and it is the BEST of the three

```
CRATONVM_ARGS="--jdk-only" bash regression-suite/run.sh
REGRESSION SUITE: 88 passed, 11 failed of 99 scheduled
```

**88/99 under `--jdk-only` against 84/99 in Compatible mode.** Four vectors
that fail in Compatible mode PASS under the policy this effort exists to
build: `RImmutableFactoryTypes`, `RJdkFunctionCombinators`,
`RJdkEnumerations`, `RServiceLoaderDoubleSource`. Real class bytes beat
synthetic natives on those four. Worth saying plainly, because the framing
everywhere else in this directory is that `--jdk-only` is the harder mode.

## Triage — first divergence for all 11, MEASURED

Each was run on both VMs and diffed. `extract()`-equivalent filtering applied:
the raw stderr is dominated by a `gc::guard` W7-84 boxing WARN repeated ~16
times per run, which swamps a naive `diff`.

| vector | first divergence | owner |
|---|---|---|
| `RJdkIntrinsics3` | `Handler.setLevel(null)`: expected `NullPointerException`, **got none — the call RETURNED** | JUL null axis |
| `RJdkBridge1` | `URI value checks threw AssertionError`, after the `new URI((String) null)` step | `net_uri_inet.rs` |
| `RJdkMapViews` | `AbstractMethodError: method java/util/Map.isEmpty()Z has no Code attribute` | interface doors |
| `RJdkOptionalShape` | `AbstractMethodError: method java/net/http/HttpRequest.version()Ljava/util/Optional; has no Code attribute` | interface doors |
| `RArrayStoreInterfaces` | `s12 Runnable[] <- Proxy(Marker)`: want `ArrayStoreException`, got no-throw, in **both** cold and hot | aastore predicate |
| `RJdkProxy` | `null for an int-returning proxy method must NPE` | proxy semantics |
| `RJdkProxyIface` | `step refusals: AssertionError: unreachable`; `checks=37` vs oracle `38` | proxy semantics |
| `RJdkForeign` | `1 of 7 steps failed: [layouts]` | FFM |
| `RForeignLayoutJdkInterfaces` | `a heap segment's scope is stable` | FFM |
| `RSimpleTimeZoneRaw` | `New_York inDaylightTime(JUL)` | tz |
| `RSslLiveSession` | `NPE: ServerSocket.getImpl() is null` at `phase=startup` | socket impl |

### The one cross-vector root cause

`RJdkMapViews` and `RJdkOptionalShape` fail with the **same** shape —
`AbstractMethodError: ... has no Code attribute` — on two unrelated types
(`java.util.Map`, `java.net.http.HttpRequest`). An abstract/interface method
is being invoked directly instead of dispatching to the concrete
implementation. That is the interface-door family (`C7-2`, `C13-1`), and
fixing it plausibly closes two vectors. **Plausibly** — not measured.

`RJdkIntrinsics3`'s failure is a precise instance of the trap
`HANDOFF-20260814` §5 already names: *"`Handler.setFilter(null)` and
`Logger.setLevel(null)` are legal where three sibling setters throw."*
`Handler.setLevel(null)` is one of the three that MUST throw, and it returns.
Do not fix it with a blanket rule — the blanket rule is what the handoff
warns produced broken paths.
