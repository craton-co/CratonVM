# `test.context.*` cluster (bean/groovy/junit/junit4/testng/web) - all tracked residuals fixed

| | |
|---|---|
| **Status** | Two independent root causes **FIXED**: constructor-parameter-annotation offset (`native-builtins/src/lang_class.rs`, branch `fix/test-context-cluster`, merged to `dev`) and `InheritableThreadLocal` not propagating through `Thread(ThreadGroup, ...)` constructors (`native-builtins/src/lang_system.rs::native_thread_start0`, branch `fix/test-context-cluster-round2`). The later Mockito/ByteBuddy residuals documented below are also **FIXED** on current `dev` by `cc8200535` / merge `0990570e9` (see `spring/spring-test-context-bean-mockito-parameter-annotations.md`): `MockitoBeanByTypeLookupIntegrationTests` is now 5/5 green, and a 95-class `bean.override.mockito` package sweep is 92 OK / 3 EMPTY with 227/227 methods passed. **Residual 1 (Groovy loader/ANTLR) is now also FIXED** (2026-07-07, branch `fix/groovy-antlr-atnconfig-layout-0707`): the "ATNConfig field-layout bug" turned out to be CratonVM's own force-registered ANTLR ATN fast-path shim applying standard-ANTLR4 slot indices to the tunnelvisionlabs-fork layout Groovy actually ships (fixed on `dev` by `50119adb`, 2026-07-05, web-cluster session), and the `groovy.*` `ClassUtils.isPresent` stub that was papering over it is now removed — all 8 groovy classes green (24/24 methods), see the rewritten Residual 1 section below. Residual 5, the JUnit parallel-execution no-result residual, is also **FIXED** on 2026-07-08 by native Spring/JUnit annotation-filter and `SpringExtension.resolveParameter` fast paths in `native-builtins/src/lib.rs`; the historical full-sweep counts above are stale for Mockito after the 2026-07-05 verification. |
| **Area** | Reflection (`Constructor.getParameterAnnotations()`); threading (`Thread.<init>` / `InheritableThreadLocal`), both real-JDK mode. |
| **Discovered** | 2026-07-04, working the `test.context.*` remaining-buglist clusters (docs 16–21 in the roadmap — those per-cluster spec files did not actually exist anywhere accessible; this doc regenerates the ground truth from a live HotSpot-vs-CratonVM sweep instead). |

## Fixed: `Parameter.getDeclaredAnnotations()` AIOOBE on non-static inner-class constructors

**Symptom:** `java.lang.ArrayIndexOutOfBoundsException: null` at `java.lang.reflect.Parameter.getDeclaredAnnotations(Parameter.java:333)`, reached via `AnnotationsScanner` from any reflective scan of a constructor parameter (Spring's `BeanOverrideUtils`, JUnit Jupiter's constructor-injection resolution, etc.). Hit 22 test methods across 9 classes; 7 are CV-unique (HotSpot passes): `SpringJUnitJupiterAutowiredConstructorInjectionTests` + the 6 `@Nested` `*ExtensionContextNestedTests` classes under `test.context.junit.jupiter.nested`.

**Root cause:** `native_method_get_parameter_annotations` (registered for both `Method` and `Constructor` — see the registration comment: CratonVM's `Constructor` reflective objects are built without the raw `annotations`/`parameterAnnotations` byte[] fields real JDK bytecode parses, so the real `getParameterAnnotations()` bytecode would return empty and these natives replace it entirely) returned an array sized to `param_annotations.len()` — the class file's `RuntimeVisibleParameterAnnotations` count, indexed against the *source-level* parameter list. For a **non-static inner class constructor**, the compiler prepends a synthetic leading parameter (the enclosing instance, `this$0`) that has no entry in that table, so the real parameter count (from the descriptor) is one more than `param_annotations.len()`. The returned array was too short and shifted: index 0 held the *first explicit* parameter's annotations instead of an empty array for the synthetic one, and the last (real) parameter's index ran off the end → AIOOBE. Confirmed with a minimal repro (`InnerCtorParamRepro.java`, kept out-of-tree) comparing a non-static vs `static` inner class constructor — only the non-static (synthetic-leading-param) case diverged from HotSpot.

**Fix:** when `param_annotations.len() < count_method_params(&method_desc)`, right-shift: build the output array to the full descriptor-length, pad the leading `synthetic_leading = param_count - param_annotations.len()` slots with empty `Annotation[]`, then copy the real rows starting at that offset. Mirrors real JDK's `Executable.handleParameterNumberMismatch`, which this native override otherwise bypasses entirely. Also clamps defensively against the (invalid/unexpected) case of *more* raw rows than descriptor params, to avoid a Rust-side out-of-bounds write.

**Blast radius (confirmed by rerun):** fixes the 7 `nested`/`SpringJUnitJupiterAutowiredConstructorInjectionTests` CV-unique classes directly (23/23 classes, 168/168 methods green in the affected slice), **plus** clears **all 6 `test.context.web` ABEND classes** (`EnableWebMvcXmlConfigTests`, `GenericXmlWebContextLoaderTests`, `MetaAnnotationConfigWacTests`, `RequestAndSessionScopedBeansWacTests`, `ServletContextAwareBeanWacTests`, `ServletTestExecutionListenerJUnitIntegrationTests` — an uncaught AIOOBE during a shared reflective bootstrap step was crashing (ABEND) the whole batch these classes shared, not just the manifesting class). Verified each individually post-fix: all OK, confirmed again in a full 249-class re-sweep. **13 of the original 25 CV-unique failures resolved by this one fix** (25 → 12).

**Note on `test.context.bean.MockitoBeanContextCustomizerEqualityTests`:** running this class alone (`KRunDebug`, direct `--java-home` invocation) is a clean `OK` post-fix — test *discovery* itself was hitting the same AIOOBE scanning `@Nested`/parameterized constructors, and the fix resolves it in isolation. But the full-suite batch re-sweep still shows it as `LOADERR`, immediately preceded in its batch by `MockitoBeanConfigurationErrorTests` (itself `LOADERR`, `junit-platform-suite` engine discovery failure — looks like an intentional-error-case test, not obviously a CratonVM bug). This smells like a **pre-existing, separate batch-order state-leak** — one class's failed discovery corrupting some static registry that the next class's discovery reads — not a regression introduced by this fix. Not investigated further here; kept in the residual count above pending a dedicated repro.

## Residual 1 (FIXED 2026-07-07) — `test.context.groovy` (7 classes) + `test.context.web.BasicGroovyWacTests`

**Symptom (historical):** `.groovy`-script-based `ApplicationContext`/`WebApplicationContext` loading in the Spring **TestContext framework** (`GenericGroovyXmlContextLoader`) failed: `XmlBeanDefinitionReader` choked trying to parse the `.groovy` script as XML (`SAXParseException: Content is not allowed in prolog`), because `native-builtins/src/net_phase_e.rs` hardcoded `ClassUtils.isPresent(name, cl)` to `false` for any `name.starts_with("groovy.")`, so `DelegatingSmartContextLoader`'s static `GROOVY_PRESENT` check never picked the Groovy loader. Removing that stub had been tried and reverted (2026-07-04) because the Groovy compiler then wedged the VM: millions of `gc::guard` out-of-bounds field read/write warnings on `groovyjarjarantlr4.v4.runtime.atn.ATNConfig` (`index=3/4` vs `num_slots=3`), no completion, under both jit-real and `--nojit`.

**Actual root cause of the ANTLR wedge (none of the doc's original hypotheses):** the 2026-07-04 session hypothesized a class-file parse gap / duplicate-classfile identity bug / superclass-chain field-count bug ("the real class needs ≥5 fields but CratonVM computed 3"). All wrong: `num_slots=3` was **correct**. Groovy's shaded `groovyjarjarantlr4` is the **tunnelvisionlabs ANTLR4 fork**, whose base `ATNConfig` has only 3 instance fields (`state`, packed `altAndOuterContextDepth`, `context`) with subclass fields (`semanticContext`, `lexerActionExecutor`, `passedThroughNonGreedyDecision`) at slots 3..5 of the `SemanticContextATNConfig`/`ActionATNConfig`/`ActionSemanticContextATNConfig` nested subclasses — unlike standard ANTLR4's 5-field `ATNConfig` (`state`/`alt`/`context`/`reachesIntoOuterContext`/`semanticContext`). The OOB reads/writes came from **CratonVM's own force-registered ANTLR ATN fast-path shim** (`native_antlr_atn_config_init` / `antlr_atn_config_snapshot` in `native-builtins/src/lib.rs`, introduced `e48e14cc` 2026-07-03 for Hibernate `FunctionTests` hot paths), which intercepted the shaded classes too but applied the standard-ANTLR4 slot indices (3/4) to the fork's 3-slot base objects. Diagnosed via `CRATONVM_DBG_OOBFIELD=ATNConfig` backtrace on a pre-fix binary; the guard's own "case B — caller-side wrong-slot dispatch, `real_field_count=Some(3)`" classification was pointing at this all along.

**Fix:** two independent parts, both on `dev`:
1. `50119adb` (2026-07-05, "Fix web cluster residual bugs" — landed independently in the web-cluster session, which is why the wedge no longer reproduced by the time this branch revisited it): taught the shim the fork's packed layout (`GROOVY_ATN_ALT_MASK`/depth-shift decoding of packed slot 1, `antlr_groovy_atn_special_slot` for subclass fields, `object_num_fields` guards). Verified by bisect-style binary hops: a dev binary built 2026-07-06 03:45 (pre-web-cluster-merge) still wedges (4.3M warnings/120s); `b02e3040` (contains `50119adb`) and everything after is clean.
2. Branch `fix/groovy-antlr-atnconfig-layout-0707`: removed the now-unneeded `groovy.*` branch from the `ClassUtils.isPresent` stub in `net_phase_e.rs` (the `jakarta.faces.context.FacesContext` branch is preserved), so Spring's Groovy detection is honest again.

**Verification (2026-07-07, Azure Linux, jit-real, frozen `cratonvm-groovyatn-0707.bin` = `dev`@`fa1c505f` + stub removal):**
- All 8 affected classes green, zero `gc::guard` warnings: `GroovySpringContextTests` 6/6, `AbsolutePathGroovySpringContextTests` 6/6, `RelativePathGroovySpringContextTests` 6/6, `MixedXmlAndGroovySpringContextTests` 1/1, `DefaultScriptDetectionXmlSupersedesGroovySpringContextTests` 1/1, `DefaultScriptDetectionGroovySpringContextTests` 1/1, `GroovyControlGroupTests` 1/1, `test.context.web.BasicGroovyWacTests` 2/2. Baseline (same binary minus stub removal) fails all 6 `GroovySpringContextTests` methods with `Failed to load ApplicationContext`.
- Direct layout probe (`ATNConfig.create(...)` for all four config shapes, reading back subclass fields via getters) byte-identical to HotSpot.
- Standalone `GroovyBeanDefinitionReader.loadBeanDefinitions(context.groovy)` + `refresh()` + `getBean()` driver matches HotSpot.
- No regression: `SpringJUnitJupiterAutowiredConstructorInjectionTests` 6/6, `DelegatingSmartContextLoaderTests` 14/14, `EnableWebMvcXmlConfigTests` 2/2, `SpringExtensionTests` 17/17 (identical pre/post-change); `cargo test --release -p cratonvm-classloading` 541+63 passed / 0 failed; `-p cratonvm-native-builtins` 2796 passed / 13 failed — the 13 (`lang_class` mirror/descriptor tests etc.) fail identically on unmodified `dev`, pre-existing feature-flag harness artifact, see `fixture-tests-need-synthetic-jdk` memory.

**Residual note (separate, pre-existing, NOT this bug):** general-purpose Groovy *runtime* execution still has open defects unrelated to the TestContext path — e.g. a reflective `Method.invoke` on a `parseClass`-compiled class returning `null` where HotSpot returns a value, and `GroovyShell.evaluate` on a multi-class script failing in `CachedMethod.compareToCachedMethod` ("Should never happen") — the same Groovy compiler-internal/metaclass state family already tracked in [`hib-proxyclassreuse-loader-blind-class-resolution.md`](hib-proxyclassreuse-loader-blind-class-resolution.md) ("Follow-up: Groovy compiler-state reuse across scripts"). The TestContext groovy bean-DSL classes do not hit those paths.

## Fixed (round 2): `InheritableThreadLocal` not propagated through `Thread(ThreadGroup, Runnable, String[, long])` constructors

**Symptom:** `IllegalStateException: Failed to retrieve ApplicationEvents for the current thread` — `ApplicationEventsHolder`'s backing store is a plain `InheritableThreadLocal`, and code that asserts on a worker/pool thread (Awaitility's default `untilAsserted` poller, JUnit 5 parallel execution, TestNG's own thread) never sees the value the test thread set. Affected: `test.context.junit.jupiter.event.JUnitJupiterApplicationEventsIntegrationTests` (`asyncConsumption`), `test.context.junit4.event.JUnit4ApplicationEventsAsyncIntegrationTests` (`asyncConsumption`), `test.context.testng.event.TestNGApplicationEventsAsyncIntegrationTests` (`asyncConsumption`) — **all now fully green (8/8, 2/2, 2/2 methods)**. `test.context.junit.jupiter.event.ParallelApplicationEventsIntegrationTests` still has a 2-method residual — see below, it's a different (JUnit5-parallel-execution) bug, not this one.

**Root cause, precisely isolated via a minimal repro** (`InheritableTLRepro2.java`/`ITLRepro3.java`/`ITLRepro4.java`, kept out-of-tree): under real-JDK mode (`--java-home`, no `--synthetic-jdk`), an `InheritableThreadLocal` value set on the parent thread propagates correctly to a child created via `new Thread(Runnable)` **or** `new Thread(ThreadGroup, Runnable)` **or** `new Thread(Runnable, String)` — i.e. any constructor where *at most one* of `{group, name}` is explicitly non-null. It fails **only** when **both** `group` and `name` are explicitly non-null simultaneously (`Thread(ThreadGroup, Runnable, String[, long])`) — which is exactly what `java.util.concurrent.Executors.defaultThreadFactory()` uses for every pooled worker, so **every** `ExecutorService`/`ScheduledExecutorService`-backed thread was affected, not just these test classes. Confirmed the child's `name`/`group`/`priority`/`contextClassLoader` are all correctly set even in the failing case — **only** `inheritableThreadLocals` is dropped, and reading it back via `NativeContext::get_field_by_name` shows `Value::Int(0)` (not `Value::Object(None)`) — i.e. whatever writes it never reaches `putfield`, leaving the raw zero-fill representation a native heap read observes (rather than the `Object(None)` a bytecode `getfield` would present).

All public `Thread` constructors delegate to the exact same private master constructor (`Thread(ThreadGroup, String, int, Runnable, long)`, JDK 25 `javap` confirms this — verified byte-identical bytecode reached regardless of entry point) which contains the `this.inheritableThreadLocals = ThreadLocal.createInheritedMap(parent.inheritableThreadLocals)` copy. Tracing the bytecode shows the *only* control-flow difference between passing/failing cases is two independent, non-adjacent forward conditional branches (`if (group != null) skip-reassignment` and `if (name != null) skip-default`) both being taken simultaneously — a synthetic repro reproducing just that shape (two independent skip-branches + a reused local-variable slot, unrelated to `Thread` entirely) did **not** reproduce it, so the defect is specific to this method's actual bytecode, not a generic "two branches" interpreter bug. None of the involved constructors are natively overridden (only the no-arg `Thread()` is), so this is **real bytecode misbehaving**, not a native-registration gap. Root-causing the exact interpreter mechanism (why *this* control-flow combination drops *only* this one `putfield`) needs opcode-level bytecode tracing that wasn't completed — tracked as a follow-up.

**Fix (pragmatic workaround, not the interpreter root cause):** `native_thread_start0` (already the native override for `Thread.start0()`, active in real-JDK mode, and already doing an analogous fixup for `contextClassLoader` propagation per the existing `TC0622` comment) now also checks, right before spawning: if the child's `inheritableThreadLocals` field reads back as `Object(None)` **or** `Int(0)`, and the parent (the thread calling `.start()`) has a non-null map, copy it via a real `ThreadLocal.createInheritedMap(parentMap)` invocation (not just aliasing the reference — the child needs its own independent copy per `InheritableThreadLocal` semantics) and write it into the child. Opt-out: `CRATONVM_INHERIT_TL_WORKAROUND=0`.

**Known trade-off:** `characteristics` (the internal flag distinguishing an explicit `Thread(group, target, name, stackSize, false)` opt-out of inheritance) isn't retained anywhere observable post-construction, so this fix can't tell "buggy" apart from "intentionally opted out". A legitimate opt-out combined with non-null `group`+`name` would incorrectly regain inheritance. That specific combination is rare in practice (the 5-arg opt-out constructor is rarely used at all); documented here pending a real interpreter-level fix that would make this workaround unnecessary.

**Repro (works standalone, no Spring needed):**
```java
static final ThreadLocal<String> TL = new InheritableThreadLocal<>();
TL.set("hello");
new Thread(() -> System.out.println(TL.get())).start();                      // "hello" — OK, always was
Executors.newSingleThreadExecutor().submit(() -> System.out.println(TL.get())); // was null, now "hello"
```

## Fixed (2026-07-05 verification): Mockito/ByteBuddy `MockAccess.getMockitoInterceptor()` failure

**Historical symptom:** `test.context.bean.override.mockito.MockitoBeanByTypeLookupIntegrationTests` failed all 5 methods with `IllegalStateException: Failed to load ApplicationContext`. After the `StringBuilder`/`ArrayStoreException` masking layer was fixed, the exposed cause was Mockito inline mock creation failing with `MockitoException: Mockito cannot mock this class: interface ...YetAnotherService`, caused by ByteBuddy's `IllegalStateException: Cannot resolve field for public abstract org.mockito.internal.creation.bytebuddy.access.MockMethodInterceptor org.mockito.internal.creation.bytebuddy.access.MockAccess.getMockitoInterceptor()`.

**Resolution:** current `dev` contains `cc8200535` (merged by `0990570e9`), documented in `spring/spring-test-context-bean-mockito-parameter-annotations.md`. The fix keeps StringBuilder/StringBuffer/AbstractStringBuilder native layout shims authoritative after Mockito JVMTI redefinition, adds the narrow compact-string/String(AbstractStringBuilder, Void) bridges real JDK code still needs, and also fixes the parameter-annotation shape issue that affected Spring bean override tests.

**Validation:** with a fresh current-`origin/dev` binary (`cratonvm-bytebuddy-mockaccess-20260705-032836`), `MockitoBeanByTypeLookupIntegrationTests` passes 5/5. A broader `--only "org\.springframework\.test\.context\.bean\.override\.mockito"` jit-real sweep over 95 classes reports `EMPTY=3 OK=92`, with 227/227 discovered test methods passed and no `ArrayStoreException`, `MockitoException`, or `Cannot resolve field` recurrence.

## Fixed (2026-07-05 verification): batch-order cascade from the Mockito failure

The old Residual 4 was a non-deterministic `LOADERR` cascade in subsequent `bean.override.mockito.*` classes after `MockitoBeanByTypeLookupIntegrationTests` failed. The 2026-07-05 package sweep ran the whole `bean.override.mockito` slice in batched JVMs (`--batch 12`) and produced `EMPTY=3 OK=92` with 227/227 methods passed. That closes the cascade for this package; if future full-suite runs show discovery corruption elsewhere, track that as a new issue with fresh evidence rather than carrying forward this resolved Mockito-triggered cascade.

## Residual 5 (FIXED 2026-07-08) - `ParallelExecutionSpringExtensionTests` no longer stalls without a result

**Historical symptom:** `test.context.junit.jupiter.parallel.ParallelExecutionSpringExtensionTests` (`@RepeatedTest(10)`, each repetition running 1000 sub-tests via JUnit5's own parallel-execution engine) previously produced no `RESULT` line before the individual timeout. The original note classified this as a genuine parallel-execution hang and mentioned a few self-recovering GC array-header warnings seen during one long run.

**Updated diagnosis:** fresh current-`dev` probes showed this was not a deadlock in JUnit's worker pool. HotSpot completed the full class in about 11.5s (`found=10 succ=10 ... status=OK`). CratonVM was making steady progress but was slow enough to look hung through shorter harness timeouts: the inner 1000-test launcher completed, just at roughly 100s per outer repetition before this fix. `--nojit` behaved similarly, and worker-thread probes showed the same single `junit-1-worker-1` shape as HotSpot, so the main issue was interpreter/native-dispatch overhead in Spring/JUnit's repeated annotation and parameter-resolution paths rather than thread scheduling.

**Fix:** add focused native fast paths in `native-builtins/src/lib.rs` for the hot Spring/JUnit annotation predicates used during the inner launcher (`PackagesAnnotationFilter.matches(String)`, the default `AnnotationFilter.matches(Class)`, `AnnotationsScanner.hasPlainJavaAnnotationsOnly(...)`, and JUnit's `AnnotationUtils.isInJavaLangAnnotationPackage(Class)`). Also add a native `SpringExtension.resolveParameter(...)` equivalent that preserves Spring's constructor scoping and `@BeanOverride` fallback while directly returning the current `ApplicationContext` for the common exact `ApplicationContext` parameter case and delegating other parameters to `ParameterResolutionDelegate.resolveDependency(...)`.

**Verification (2026-07-08, Azure Linux, real-JDK mode, binary `/data/data/cratonvm-bins/cratonvm-junitparallel-20260708-final`):**
- Focused inner probe: `JUnitParallelOne` passed `found=1000 succeeded=1000 failed=0 skipped=0 ms=49426`.
- Full target through the shared KRun harness: `RESULT org.springframework.test.context.junit.jupiter.parallel.ParallelExecutionSpringExtensionTests found=10 succ=10 fail=0 skip=0 abort=0 ms=513464 status=OK` under a 900s guard.
- Build check: `cargo build --release -p cratonvm-cli` succeeded; the final binary was copied to `/data/data/cratonvm-bins/cratonvm-junitparallel-20260708-final`.

This closes the old suite-correctness residual: the class now reaches a clean KRun `RESULT` instead of timing out without one. It remains much slower than HotSpot, but that is a broad performance gap rather than the previously documented no-result hang.

## How this doc's ground truth was produced

The roadmap task referenced `out/rerun-20260701-143455/REMAINING-BUGLIST.md` and six per-cluster spec docs (`16-test-context-bean.md` … `21-test-context-web.md`) with pre-counted class lists (7/6/10/4/3/8). None of those files exist in the local checkout, the shared Azure `/data/cratonvm` checkout, or anywhere in git history across all branches — they were referenced but never actually written/committed. Ground truth here was regenerated directly: a fresh `cratonvm.exe` (dev `4b9f3fc17` + this branch's fix) run against the shared `spring-suite-runner` harness, `--only` filtered to the six packages (249 classes total), diffed against a same-slice HotSpot baseline through the identical JUnit-Platform launcher. Real counts (25 CV-unique, not 38) differ from the stale estimate; see the per-cluster breakdown above.
