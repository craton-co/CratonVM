# Fixed suite bugs (archive)

**Non-normative history.** These are the original per-suite bug/crash reports for
defects that are now **FIXED on `dev`**, moved here out of the active trackers so
that `docs/known-issues/` holds only **open** items. Kept for traceability (repro
+ root cause + fix commit); do not cite as current behaviour.

| Doc | Fix |
|---|---|
| `h2-suite-bugs/h2-jitban-schema-not-found-on-reconnect-FIXED.md` | `13055f75c` — two general x64 defects, not H2 bugs: the compact-layout `String.coder`/`hash` offsets were +4 (so `length()` evaluated `value.length >> (hash & 31)` once a hash cache was populated, and H2 persisted an empty schema name into its own metadata), and a reload-elision mirror leaked across a control-flow join. `Schema  not found` is extinct across 218 classes. `HIB-LONGTAIL.1` still stands on three remaining classes — `known-issues/h2/h2-jitban-residuals-20260726.md`. |
| `h2-suite-bugs/bug-h2-testfilesystem-testconcurrent-async-hang-FIXED.md` | `2eeff06b8` + `c6c1d233d` (after `4afa20cea` and `74b709d8e`) — six uncached `getenv` reads on interpreter hot paths (6,901,759 calls → 1,070), three redundant `resolve_method_ref` calls per invoke (6.78M → 1.49M), and a class-name prefilter for the native-registry digest. The remaining performance wall is one prefix/sub-test pair, carried in `known-issues/h2/h2-jitban-residuals-20260726.md`. |
| `bc-asn1-pkcs12test-indefinitelengthinputstream-stackoverflow-FIXED.md` | `codex/fix-bc-pkcs12-stackoverflow-20260709-001` - base `InputStream.read([BII)` no longer redispatches explicit `super.read([BII)` to the receiver override; reduced VM probe covers the BC recursion shape and normal virtual override dispatch. |
| `bug-03-priorityqueue-boxed-ordering.md` | `936b8e19` — verified vs HotSpot |
| `crash-01-arraylist-capacity-oom-abend.md` | `fix/oom-array-alloc-abend` (base `0e3f0398`) |
| `crash-02-native-capacity-ctor-abort-family.md` | `da58ff4e` — verified vs HotSpot |
| `crash-03-rsocket-payloadutils-jit-sigsegv.md` | `957270c8` — verified |
| `spring-bug-02-kotlin-reflect-builtins-null.md` | `cf58ea48` (SB-15) — symptom resolved |
| `spring-bug-03-stream-iface-no-code-attribute.md` | `4c5a4d09` (merge `d75f7716`) — verified |
| `spring/spring-bug-04-junit-timeout-interceptor-double-proceed.md` | `fc20e970` — `compare_via_compare_to` throws CCE for non-`Comparable`; `ValueCodeGeneratorTests` 41/41 vs HotSpot. "Interceptor invoked twice" was a mask, not threading/MH. |
| `spring-bug-05-dynamic-proxy-module-system.md` | `16f832c0` (merge `d75f7716`) |
| `spring-bug-12-hashmap-view-spliterator-int16.md` | `fix/spring-bug-10-11` — validated |
| `bug-A-arraylist-sublist-copy-not-view.md` | `9a535e85` (off dev `b8908…`) |
| `spring/springsuite-0620-toarray-referencepipeline-recursion.md` | `8795b88d` — `ReferencePipeline.toArray(IntFunction)` native re-entered no-arg `toArray()` → `StackOverflowError`; was the `MergedAnnotations` hang + bug-06 fam6 "~2 GB OOM" and blocked the whole JUnit suite. `MergedAnnotationsTests` 174/178, `AnnotationUtilsTests` 72/72. |
| `spring/springsuite-0619-unsafe-offheap-directbuffer.md` | `3b16e985` (bug-A) + re-verified 2026-06-20 (bug-A2 also resolved) — `PooledDataBufferTests` 10/10, `LeakAwareDataBufferFactoryTests` 2/2. |
| `SC-map-multivaluemap-family.md` | RC-1 keySet `contains`→`containsKey` + RC-2 LHM `putIfAbsent` null-replace (earlier on dev) + RC-3 `Map.equals` foreign-Map operand via virtual dispatch (`e115b0bd`). All 3 native-collections Map bugs verified vs HotSpot (JDK 25, `test_classes/MapEqRepro` 15/15). RC-4/RC-5 are non-Map ByteBuddy/Mockito handoffs (bug-E). |
| `SC-aot-runtimehints-resource-count.md` | `Stream.distinct()` (`native_stream_distinct`) deduped via shallow `values_equal` → value classes/records never collapsed → over-counted resource globs (8 vs 5). Now dedups via `list_element_matches` (real Java `equals`), `029f2c87` (merge `cf269fc5`). Verified vs HotSpot (JDK 25, `test_classes/DistinctEquals` 5/5). 4 non-`distinct()` writer tests = unconfirmed separate residual (re-triage if they fail). |
| `restclientextensions-mockk-verify-ptr-arg.md` | Current `dev` (`89db75ae`) no longer reproduces the mockk `ParameterizedTypeReference` verify mismatch; exact causal commit not isolated. Verified 2026-07-01: `RestClientExtensionsTests` 5/5 in real-JDK, JIT-on mode, and also 5/5 with `CRATONVM_XT_JIT_ROOT_SCAN=0`. |
| `bug06-fam5-reflection-getdeclaredmethod-null.md` | ✅ CLOSED 2026-07-02 — the `getDeclaredMethod on null` ×28 aggregate is **extinct**: 0 instances in the clean full re-runs (dev `d707c97e` 2026-06-30, `f7506e02` 2026-07-01) and in a fresh 196-class nojit+jit sweep of the reflection/annotation surface on dev `ffb247e5`; `Refl5` probe ==HotSpot both modes. Was a cross-family cascade (fam1/3/4 + `toArray` recursion + bug-04 GC + bug-05 generics), all sources fixed. Repro kept at `repros/bug06-fam5-reflection-null/Refl5.java`. |
| `keycloak/keycloak-previewfeatures-ispreviewenabled-native.md` | Added `jdk/internal/misc/PreviewFeatures.isPreviewEnabled()Z` native (`native-builtins/src/lib.rs`, `register_essential_natives`), returning `false` to match HotSpot's no-`--enable-preview` default. Smoke-verified on Azure with JDK 21: HotSpot and CratonVM both return `false`. Full Azure rerun `craton-azure-nonpassed-dev-20260703-previewfeatures-fixed-01` has 0 `PreviewFeatures.isPreviewEnabled` occurrences in notes/stderr, so the original 1044/1044 launcher-wide native crash is fixed. Post-fix residuals are tracked in `docs/known-issues`. |
| `keycloak/keycloak-crypto-fips1402-cryptoprovider-serviceloader.md` | `Provider.containsKey(Object)`, `Provider.get(Object)`, and `Provider.getServices()` now mirror the provider side tables populated by `Provider.put`; Keycloak FIPS `ServiceLoader` resolves `org.keycloak.crypto.fips.FIPS1402Provider` (`providerCount=1`) and representative suite classes no longer fail at `CryptoInitRule.before`. |
| `keycloak/keycloak-fips1402-assumption-aborts-classification-FIXED.md` | JUnit assumption aborts are `SKIP` or `PARTIAL`, not `FAIL`; full 21-class HotSpot/CratonVM Azure comparison has identical 11/3/7 status distribution and zero failures. |
| `keycloak/keycloak-universal-classpath-post-enterprisedb-residuals.md` | `codex/fix-keycloak-post-enterprisedb-residuals-20260708-001` - the universal classpath generator now adds filtered Selenium/UI plus Quarkus Maven resolver closures, and the suite runner puts selected module output dirs before `kc-runner` for pathing jars. HotSpot and CratonVM both reach the same local Docker/Testcontainers environment failure instead of the old `WebDriver`, `BootstrapMavenContext.config()`, or `remote-providers` failures. |
| `hibernate-bytearraymapping-stackwalk-gc-corruption.md` | `codex/hib-bytearray-stackwalk-gc-20260703` — native constructor helpers pin object constructor args across allocation/class-init GC; `ByteArrayMappingTests` 2/2 no longer SIGSEGVs. |
| `hib-proxyclassreuse-loader-blind-class-resolution-FIXED.md` | `codex/fix-proxyclassreuse-residuals-20260708-143410` - loader-aware proxy class reuse stays fixed, and the residual Groovy/BeanShell namespace tail is retired: `ProxyClassReuseTest` 3/3, `GroovyBeanDefinitionReaderTests` 36/36, `BshScriptFactoryTests` 18/18, component-scan probe green, and related `InPredicateTest` default-JIT run green on the final unique Azure binary. |
| `hib-jpalargeblobtest-bulk-read-timeout-and-loaderr-artifact-FIXED.md` | `codex/fix-hib-jpalargeblob-loader-poison-20260709-001` - corrected the remote rerun's classloader-poisoning hypothesis as a fork-per-class/ENOSPC artifact, fixed the real `JpaLargeBlobTest` 200 MiB byte-at-a-time Blob stream timeout with an exact native bulk-read fast path, and cleared stale loader namespace ids on VM reset. |
| `jit-inline-alloc-array-header-corruption-hibernate-batch.md` | `74dc80b8` — shared GenerationalHeap allocation now reserves, zeroes, and writes the object/array header while holding the arena lock; focused allocation-ordering regression + `cratonvm-gc --lib` passed. |
| `keycloak/keycloak-model-infinispan-globalconfiguration-isclustered-nosuchmethod-FIXED.md` | `fix/kc-infinispan-isclustered-nosuchmethod-20260706` — `GlobalConfigurationBuilder.build()`'s identity-wrapper native (`native-builtins/src/infinispan_local.rs`) removed so real bytecode constructs a genuine `GlobalConfiguration` instead of relabeling the Builder; unblocked all 37 `testsuite/model` `CRASH` rows. New residual `ConfigurationBuilder`/`Configuration` `ClassCastException` tracked separately. |
| `wildfly-jboss-logmanager-geteffectivelevel-null-loggernode.md` | `a3728860` ("Fix WildFly non-passed suite blockers") — force-native gates in `interpreter.rs`/`vm_exec.rs` already covered `getEffectiveLevel`/`isLoggable` on `org/jboss/logmanager/Logger`; original repro binary predated the fix by ~12h same day. Re-verified 2026-07-06 against real `jboss-logmanager`/`wildfly-elytron-x500-cert-acme` jars: `Class.forName(AcmeClientSpi)` completes cleanly under real-JDK+JIT, matching HotSpot. |
| `wildfly-jboss-module-loadservice-null-moduleclassloader.md` | `5c6c6976` (registered `Module.loadService` + forced `ModuleClassLoader` resource natives) + follow-up same day (`fix/serviceloader-jboss-instantiate-20260706`) — a "still open" residual from the first pass turned out to be a test-harness gap (missing `-mp`/`CRATONVM_JBOSS_MP_ROOT`), not a VM bug; also found and fixed a real latent bug where `load_provider_class` tried a context-free `Class.forName` before the loader-scoped path, which could poison `ClassState` on a wrong-context first attempt. Verified end-to-end with real `jboss-modules.jar` + full WildFly 32.0.1 module tree: `count=1` matching HotSpot for `org.jboss.as.jmx`'s `Extension` provider; plain classpath `ServiceLoader` re-verified unaffected. |
| `keycloak/keycloak-model-infinispan-configurationbuilder-classcastexception.md` | `fix/kc-infinispan-configurationbuilder-cce-20260706` — same identity-wrapper bug shape one step deeper: `ConfigurationBuilder.build()`'s native shim removed after reworking `native_dcm_define_configuration` to read size/ttl via `Configuration`'s real `memory().maxCount()`/`expiration().lifespan()` accessors (`invoke_virtual`) instead of a raw synthetic slot index. New residual (Netty `PlatformDependent0` reflective `setAccessible` gate) tracked separately. |
| `wildfly-processenvironment-environ-native.md` | `a3728860` ("Fix WildFly non-passed suite blockers") — real-JDK essential path already registers `ProcessEnvironment.environ()[[B` and no-ops `ProcessImpl.init()V`; original repro binary predated the fix by ~12h same day. Re-verified 2026-07-06 with a standalone ProcessBuilder.environment()/start() probe: env-size and exit code match real HotSpot exactly, no UnsatisfiedLinkError. |
| `wildfly-standalone-managed-server-boot-fails-under-surefire-fork.md` | `fix/wildfly-surefire-boot-20260711` — `create_constructor_object`/`create_method_object`/`create_field_object` (`native-builtins/src/lang_class.rs`) held `obj`/`class_mirror`/etc. as unpinned `ObjectRef`s across GC-triggering classloading calls; a moving GC mid-construction (reliably triggered by WildFly's ServiceLoader-based extension bootstrap, ~500-750 module jars) corrupted the returned reflection object, surfacing as `Constructor.newInstance: no declaring class` for several `Extension` SPI providers (Elytron, IO, SecurityManager, clustering), which cascaded into `AbstractControllerService`'s `this.controller is null` NPE crashing server boot before `server.log` was ever written — 96% of `testsuite/integration/basic` failures in round 6. Fixed by pinning + re-reading forwarded references in all three constructors (6/10 sample). Two follow-up sessions found + fixed 4 MORE sites of the identical pattern: `native-io/src/stream_decoder.rs::alloc_stream_decoder`/`stream_encoder.rs::alloc_stream_encoder` (9/10 sample), then `native-builtins/src/jboss_module_loader.rs::native_module_load_service[_from_caller_module_loader]` and `native-builtins/src/service_loader.rs::native_sl_iterator` (the latter caught live via `CRATONVM_DIAG_SERVICELOADER=1`: a module-scoped provider lookup correctly found its target class, but a *later*, separate call site's `Constructor.newInstance` failed with the exact same corruption). After 6-7 total sites across 3 sessions, the residual is smaller but still open (8-retry sample: 4/8 clear, 3/8 original NPE, 1/8 still a related `WFLYCTL0153` failure) — tracked as a long-tail bug class (recommending a systematic static-analysis sweep, not further one-off chasing) plus a separately-tracked STW JIT-takeover stall, both in `docs/known-issues/wildfly-parallel-boot-stale-objectref-residual.md`. |
| `elasticsearch-suite/elasticsearch-engine-merge-policy-hangs.md` | A5 root-snapshot scan fixed by `cbae5cd0`; the later corrupt-header/RRWL residual is covered by `0c6cbd58` (`weakref_null_referents_pre_gc` watches Reference objects + `GenerationalHeap::is_live_young_survivor`) and follow-up crawl-regime fix `2072699b`. Revalidated 2026-07-08 with unique Azure binary `cratonvm-es-engine-residuals-baseline-20260708`: `RwlReadTearingProbe 8 8000` completed 4/4; direct ES class rerun was blocked by missing compiled Elasticsearch checkout on the host. |
| `http-client-reactor-windows-timeout-linux-epoll-gap-FIXED.md` | Retired 2026-07-08 on current `dev@d49ce503`: unique Azure binary `cratonvm-reactor-http-timeout-20260708-170805.bin` passes `ReactorClientHttpRequestFactoryTests` 10/10 in real-JDK Spring `KRun`; old Linux EPoll gap is obsolete after NIO selector routing/fixes. |
| `groovy-compilationunit-phaseoperation-abstractmethoderror-FIXED.md` | `36e0cef1` / merge `a3d771c2` - lambda proxy no-code fallback now invokes concrete default methods on the proxy's functional interface; Spring Groovy `PhaseOperation.doPhaseOperation` AME gone. |
| `springboot/springboot-batch-jdbc-mergedannotation-isdirectlypresent-abstractmethoderror-FIXED.md` | `61eb24f94` - receiver forwarding after cold instance-field resolution prevents moving-GC stale receivers from dispatching a real Spring annotation object as an abstract `MergedAnnotation`; focused JIT and `--nojit` Batch JDBC reruns contain zero target `AbstractMethodError` occurrences. |
| `tls-ocsp-clientcert-validation-not-enforced-FIXED.md` | Retired 2026-07-08: OCSP, client-cert identity plumbing, SSLEngineResult/HttpsServer, Linux native teardown, and TLS socket/session residuals are fixed on `dev`; remaining Tomcat deferred client-auth and TLS 1.2 DHE failures are dependency limitations requiring a TLS-backend change, not open CratonVM bugs. |
| `linkedcaseinsensitivemap-this0-deserialization-FIXED.md` | `2c008f78` - `HashMap.readObject` replay uses `evict=false`, so anonymous `LinkedHashMap` subclasses do not invoke `removeEldestEntry` before their synthetic `this$0` outer reference is restored; `RSerial`, `MimeTypeTests`, and `MediaTypeTests` pass. |

**Not moved — partial fixes with open residuals** (left in their suite folders / `docs/known-issues/`):
- `spring-suite/crash-reports-2026-06-16/bug-05-generics-fieldtypesignature-cce.md`
  — wildcard-bound `Type[]` fixed (`d01345d1`); real-`ParameterizedType` residual open.
- `spring-suite/bugs/spring-bug-09-collection-layout-probe-oob.md`
  — crash fixed (`5941addd`); a separate residual hang is still open.
- `docs/known-issues/springsuite-0619-getbeanclassname-bean-filter.md`
  — bug-B bean-filter fixed (`3b16e985`); bug-B2 CGLIB **method-injection** null-instance still
  open (`LookupMethodTests` 0/7, "Target object must not be null").
